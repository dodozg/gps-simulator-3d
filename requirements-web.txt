# Web kontrolni centar (#12) — backend. Engine je i dalje samo numpy; ovo je tanki
# FastAPI adapter oko postojećih modula. Uključuje matplotlib (requirements-plots)
# jer backend poslužuje skyplot (serialize uvozi enu_azel iz skyplot.py).
-r requirements-plots.txt
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27          # za TestClient (test_web_backend.py)
